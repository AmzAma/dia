import './library/jquery.js';
import { baseUrl } from './library/config.js';
const baseUsl = "http://localhost:8808"
  (function () {
    let id = location.search.split('=')[1];
    console.log(baseUrl);
  })();